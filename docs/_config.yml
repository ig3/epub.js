# Site settings

# Build settings
markdown: kramdown
theme: minima
baseurl: /epub.js

plugins:
  - jekyll-redirect-from

kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter_opts:
    disable : true

github: [metadata]
